{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "ZenBox main window capability — core IPC and shell-open for the OAuth consent page only.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    "updater:default",
    "process:allow-restart",
    "dialog:default",
    "notification:default"
  ]
}
